module Main exposing (..)

import CtorMulti exposing (Multi(..))

describe : Multi -> String
describe v =
  case v of
    CtorMulti.Wrap s -> s

main : List Scroll
main =
  [ scroll { name = describe (CtorMulti.Wrap "x"), glyphs = [] } ]
