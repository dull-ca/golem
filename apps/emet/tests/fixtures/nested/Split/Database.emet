module Split.Database exposing (Config, config, container)

type Config = Config { container : String }

config : Config
config = Config { container = "mysql" }

container : Config -> String
container (Config spec) = spec.container
